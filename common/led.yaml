platform: monochromatic
name: LED${id}
output: ${output}
effects:
  - pulse:
  - pulse:
      name: "Fast Pulse"
      transition_length: 0.5s
      update_interval: 0.5s
  - pulse:
      name: "Slow Pulse"
      update_interval: 2s
  - strobe:
